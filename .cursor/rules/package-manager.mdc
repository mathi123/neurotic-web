---
description: Package manager and environment setup
alwaysApply: true
---

# Package Manager

Use pnpm (defined in `package.json`). Use pnpx instead of npx.

```bash
# Install dependencies
pnpm install

# Run a package binary
pnpx <command>
```
